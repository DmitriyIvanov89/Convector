package ordersparser;

public class Producer implements Runnable {
    @Override
    public void run() {
        // Считываение данных из файла построчно и передача в очередь(BlockingQueue<Model> queue)
    }
}
